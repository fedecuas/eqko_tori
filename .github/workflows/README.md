# .github/workflows

CI/CD — pendiente hasta que exista código real en `services/` o `apps/dashboard`. Conventional
Commits obligatorio (`feat`, `fix`, `refactor`, `test`, `chore`, `docs`, `ci`) desde el primer
commit, ver `../../CLAUDE.md` sección 5 y el estándar `eqko-agents-architecture` sección 7.
