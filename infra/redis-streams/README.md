# infra/redis-streams

Config de los streams del Event Channel, sus consumer groups y la DLQ (Módulo 3 del roadmap,
`../../CLAUDE.md`).

Streams esperados (naming en `CLAUDE.md` sección 5): `leadgen.place_extracted`,
`leadgen.lead_qualified`, `leadgen.message_drafted`, `leadgen.lead_dispatched`.

Sin provisionar todavía.
